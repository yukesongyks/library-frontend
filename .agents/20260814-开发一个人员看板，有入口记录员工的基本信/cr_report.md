# Code Review Report — 系分设计评审

> **Change** `人员看板-系分设计` · **分支/Commit** `AI/task-DEV-...` / `无代码变更` · **日期** `2026-08-14` · **审查者** AI
>
> **说明**：当前阶段为**设计文档评审**，仓库中尚无 Java 代码变更。本报告按 `dtazziboot-java-code-review` 框架适配系分设计文档的结构化评审。等级映射：P0=阻塞性问题、P1=推荐修复、P2=参考改进。

---

## 1. 审查范围

| 项 | 值 |
|----|-----|
| 审查类型 | 系分设计文档评审 |
| 文档 | `.agents/20260814-开发一个人员看板，有入口记录员工的基本信/design.md` |
| 文档行数 | 895 行 |
| `.java` 文件数 | 0（无代码变更，设计阶段） |

---

## 2. 问题计数

| P0 | P1 | P2 |
|----|----|-----|
| 2 | 3 | 2 |

---

## 3. Step 2 — 功能（REQ）

### REQ-1: 人员基本信息管理（F01）
| Scenario | 结果 | Spec证据 | 代码证据 | 说明 |
|----------|------|----------|----------|------|
| 记录员工姓名、工号、部门、岗位、联系方式等基础信息 | ✅ | §5.1.1 employee 表结构含 name/employee_no/department/position/mobile/email 等字段 | design.md L289-302 | 字段覆盖完整，含必要约束 |

### REQ-2: 增删改查 CRUD（F02-F05）
| Scenario | 结果 | Spec证据 | 代码证据 | 说明 |
|----------|------|----------|----------|------|
| 创建/查询/修改/删除员工 | ✅ | §5.1.2 W01-W05 接口设计 | design.md L321-450 | 接口覆盖完整，含分页查询 |

### REQ-3: 批量导入人员（F06）
| Scenario | 结果 | Spec证据 | 代码证据 | 说明 |
|----------|------|----------|----------|------|
| 支持 Excel/CSV 格式批量导入 | ⚠️ | §5.2.2 W07 入参仅写 .xlsx | design.md L543 | CSV 格式在需求中提及但接口仅定义 .xlsx；时序图与接口设计未覆盖 CSV 处理 |

### REQ-4: 白名单校验导入（F07）
| Scenario | 结果 | Spec证据 | 代码证据 | 说明 |
|----------|------|----------|----------|------|
| 导入时校验白名单，仅白名单内人员可导入 | ✅ | §5.2.3.2 R11/R12 定义 STRICT/SKIP 模式 | design.md L676-687 | 双模式设计合理，异常场景覆盖 |

### REQ-5: 成本预算记录（F08-F09）
| Scenario | 结果 | Spec证据 | 代码证据 | 说明 |
|----------|------|----------|----------|------|
| 记录每个人员的成本预算信息，支持编辑 | ✅ | §5.3 budget 表结构 + W12/W13/W14/W15 | design.md L691-828 | 预算表含金额/已使用金额/币种等字段 |

### REQ-6: 导入模板下载（F10）
| Scenario | 结果 | Spec证据 | 代码证据 | 说明 |
|----------|------|----------|----------|------|
| 提供标准导入模板下载 | ⚠️ | §5.2.2 W08 仅定义路径未定义模板格式 | design.md L582-585 | 缺少模板列结构定义，后续实现时容易与导入解析逻辑不一致 |

---

## 4. Step 3 — 可读性检查

> 无 Java 代码：**N/A（设计文档评审）**。

| 结果 | 说明（违规写 Ax.x 与 `path:行`） |
|------|--------------------------------|
| ✅ | 文档结构清晰，分层合理，Mermaid 图表述完整 |
| ✅ | 接口表格与数据模型表格格式统一，可读性好 |
| ✅ | 时序图规范，业务流程清晰 |

---

## 5. Step 4 — 可靠性检查

| 域 | 参考 | 结果 | 等级 | 说明（列命中 ID 或「已扫无命中」） |
|----|------|------|------|-------------------------------------|
| 可靠性 | `reliability-checklist.md` G1–G17 | ⚠️ | P0–P1 | 见下方清单 |
| 安全 | `security-checklist.md` S1–S10 | ⚠️ | P1 | 见下方清单 |
| Bug 模式 | `bug-pattern-checklist.md` B/M/I（120） | N/A | — | 无 Java 代码，不适用 |

### 可靠性发现

| ID | 状态 | 等级 | 说明 |
|----|------|------|------|
| G1.1 | N/A | — | 设计阶段无并发代码；唯一索引策略可接受 |
| G2.1 | ⚠️ | P0 | **导入接口无幂等键设计**：W07 批量导入未定义幂等机制（如导入批次号），重复提交同一文件可能导致重复数据 |
| G3.2 | ⚠️ | P1 | **事务范围未明确**：导入操作时序图中，批量插入在事务内，但文件解析在上游，事务边界未明确定义 |
| G4.1 | ✅ | — | SQL 业务逻辑合理，无复杂子查询 |
| G8.1 | ⚠️ | P1 | **部分失败场景处理不明确**：异常场景说"整体事务不中断"（§5.2.3.1 L669），但并发控制写"事务保证数据一致性"（§5.2.3.1 L674），两处描述矛盾 |
| G10.2 | ✅ | — | 接口设计兼容性良好 |
| G11.3 | ⚠️ | P1 | 入参空值校验设计已提及但未明确统一校验框架 |
| G14.2 | ⚠️ | P1 | **多租户设计不完整**：tenant_id 预留但未定义从何处获取当前租户上下文 |
| G15.1 | ✅ | — | 新增表，向前兼容 |
| G16.1 | ⚠️ | P1 | **关键指标埋点设计较笼统**：§7.1 提及监控但未定义具体业务指标 |
| G17.1 | ⚠️ | P1 | **导入功能开关**：§7.3 提及功能开关但未定义具体实现方式（配置中心/数据库开关） |

### 安全发现

| ID | 状态 | 等级 | 说明 |
|----|------|------|------|
| S1.1 | N/A | — | 设计阶段，无具体 SQL 代码 |
| S7.1 | ⚠️ | P1 | 文件上传仅限制了 10MB 大小，未定义文件后缀白名单校验逻辑 |
| S8.1 | ⚠️ | P1 | **权限控制设计不完整**：§6.4.2.2 提到"建议仅管理员/HR角色可访问"，但未定义角色-权限模型及接口级鉴权方案 |
| S9.1 | N/A | — | 无密钥硬编码问题 |
| S9.2 | ✅ | — | 已提及日志脱敏 |
| S9.3 | ⚠️ | P1 | 手机号/邮箱加密存储方案未定义密钥管理、加解密时机、查询处理方式 |

---

## 6. Step 5 — 自定义扩展检查

| 域 | 参考 | 结果 | 等级 | 说明（列命中 ID 或「未启用自定义规则」） |
|----|------|------|------|------------------------------------------|
| 自定义扩展 | `customized-checklist.md` U* | N/A | — | N/A(未启用自定义规则) |

---

## 7. 结论

- **合并建议**：修复后合并（P0 问题需在编码前修正设计，P1/P2 问题建议在实现阶段同步修复）
- **P0**：
  1. **BUDGET_004 错误码未定义** — §5.3.3 业务规则 R16 引用了 BUDGET_004，但错误码表（§5.3.2 W12）仅列出 BUDGET_001~003，缺少 BUDGET_004 定义
  2. **删除员工时关联预算数据处理策略缺失** — §5.1.1 W02 提到"删除时检查关联预算数据"，但未定义处理方式（拒绝删除/级联删除/置空）

- **P1**：
  1. **导入事务处理策略矛盾** — 部分成功 vs 整体事务回滚，设计不一致
  2. **敏感数据加密方案不完整** — 手机号/邮箱加密存储缺少密钥管理、查询处理方案
  3. **乐观锁 version 字段未在表结构中体现** — 提及但表定义中无 version 字段

- **P2**：
  1. **W05 与 W06 接口职责重叠** — 分页查询已有 keyword 搜索能力，额外 search 接口冗余
  2. **CSV 格式支持未在接口定义中体现** — 需求提及但接口仅定义 .xlsx

- **一句话**：设计文档整体结构完整、功能覆盖全面，但存在 2 处阻塞性缺陷（错误码缺失、关联数据处理策略缺失）和 3 处推荐修复项，建议在编码前修正设计缺陷，确保实现可靠性。

---

## 7.1 问题片段（必填）

> 设计文档评审，无 Java 代码片段。以下为设计文档中对应的问题位置。

### P0-1: BUDGET_004 错误码未定义

- **P0** `BUDGET_004 缺失` `design.md:L818` — §5.3.3 业务规则 R16 引用了 BUDGET_004，但错误码表（§5.3.2 W12）仅列出 BUDGET_001~003

**设计文档片段**（design.md:812-818）：
```
| R13 | 关联员工必须存在 | 创建/更新时 | 返回 BUDGET_002 |
| R14 | 同一员工+年度+预算类型唯一 | 创建时 | 如已存在，走更新逻辑 |
| R15 | 预算金额不能为负数 | 创建/更新时 | 返回 BUDGET_003 |
| R16 | 已使用金额不能超过预算金额 | 更新时 | 返回 BUDGET_004 |  ← 引用未定义错误码
```

**错误码表**（design.md:744-751）：
```
| BUDGET_001 | 该员工该年度该类型预算已存在 |
| BUDGET_002 | 员工不存在 |
| BUDGET_003 | 预算金额不能为负数 |
| ← BUDGET_004 缺失 |
```

### P0-2: 删除员工时关联预算数据处理策略缺失

- **P0** `关联数据处理策略缺失` `design.md:L400` — W02 业务规则提到"删除时检查关联预算数据"，但未定义处理策略

**设计文档片段**（design.md:400-401）：
```
- **业务规则**: 逻辑删除，将 is_deleted 置为1；删除时检查关联预算数据
```

### P1-1: 导入事务处理策略矛盾

- **P1** `事务策略矛盾` `design.md:L669 vs L674` — 两处描述冲突

**片段1**（design.md:669）：部分成功模式
```
| 部分行导入失败 | 整体事务不中断，记录失败行及原因，返回成功/失败计数 |
```

**片段2**（design.md:674）：整体事务模式
```
- 控制策略：导入操作采用逐行校验 + 批量插入，利用数据库事务保证数据一致性。
```

### P1-2: 敏感数据加密方案不完整

- **P1** `加密方案不完整` `design.md:L867` — 手机号/邮箱加密存储缺少具体方案

**设计文档片段**（design.md:867）：
```
- 手机号、邮箱等个人信息建议加密存储（AES-256）
```

### P1-3: 乐观锁 version 字段未在表结构中体现

- **P1** `version字段缺失` `design.md:L828` — 提及但未定义

**设计文档片段**（design.md:828）：
```
- 控制策略：使用数据库唯一索引...使用乐观锁（version字段可选）防止并发覆盖
```

### P2-1: W05 与 W06 接口职责重叠

- **P2** `接口重叠` `design.md:L212-213` — W05 已有 keyword 搜索，W06 独立搜索接口冗余

### P2-2: CSV 格式未在接口定义中体现

- **P2** `CSV未覆盖` `design.md:L543` — 需求提及 CSV 但接口仅定义 .xlsx

---

## 8. 修复任务列表

### P0

- [ ] **P0** `design.md:L744-751` — 补充 BUDGET_004 错误码定义（"已使用金额不能超过预算金额"）
- [ ] **P0** `design.md:L400` — 明确删除员工时关联预算数据的处理策略（拒绝删除/级联删除/置空 employee_id）

### P1

- [ ] **P1** `design.md:L669 vs L674` — 统一导入事务处理策略：明确是"逐行校验+全部成功才提交"还是"部分失败仅记录不中断"
- [ ] **P1** `design.md:L867` — 补充手机号/邮箱加密存储方案：密钥管理、加解密时机、查询时解密策略
- [ ] **P1** `design.md:L828` — 在 budget 表结构中补充 version 字段定义（乐观锁）

### P2（可选）

- [ ] **P2** `design.md:L212-213` — 评估合并 W05 和 W06，或明确 W06 的差异化定位（如全文搜索）
- [ ] **P2** `design.md:L543` — 在 W07 接口定义中补充 CSV 格式支持说明