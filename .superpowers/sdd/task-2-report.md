# Task 2 Report: 数据模型、表结构与种子数据 (Data Model, Schema & Seed Data)

Status: **DONE** (with one documented, minimal deviation — see below)

## Files written (15 total)

Overwritten (were Task-1 placeholders `SELECT 1; -- placeholder`):
- `library-backend/src/main/resources/schema.sql` — 6 tables (department, business_line, project, employee, labor_cost, project_cost) + 2 indexes, **plus 2 header lines: see Deviation**
- `library-backend/src/main/resources/data.sql` — seed data verbatim per brief
  - departments: 2 (研发部/DEV-DEPT, 产品部/PM-DEPT); business lines: 2 (金融科技线/FINTECH, 数字企业线/DIGITAL)
  - projects: 4 via explicit ids (1 核心交易系统/TRADE budget 1000000.00, 2 数据中台/DATAPLT 400000.00, 3 客户门户/PORTAL 600000.00, 4 运维支撑平台/OPSPLT 400000.00)
  - employees: 4 (P001 张三 DEV, P002 李四 TEST, P003 王五 PM, P004 赵六 OPS)
  - labor_cost: 24 rows (4 employees × 6 months 2025-01..2025-06) → total 414000.00
  - project_cost: 24 rows (4 projects × 6 months) → total 1590000.00
- Entities (Lombok `@Data` + `@TableName`, `@TableId(type = IdType.AUTO)`, camelCase↔snake_case mapping, verbatim from brief):
  - `src/main/java/com/library/cost/entity/Department.java` (@TableName("department"): id, name, code)
  - `src/main/java/com/library/cost/entity/BusinessLine.java` (@TableName("business_line"): id, name, code)
  - `src/main/java/com/library/cost/entity/Project.java` (@TableName("project"): id, name, code, businessLineId, departmentId, budgetAmount(BigDecimal))
  - `src/main/java/com/library/cost/entity/Employee.java` (@TableName("employee"): id, name, employeeNo, departmentId, role)
  - `src/main/java/com/library/cost/entity/LaborCost.java` (@TableName("labor_cost"): id, employeeId, projectId, month, amount(BigDecimal))
  - `src/main/java/com/library/cost/entity/ProjectCost.java` (@TableName("project_cost"): id, projectId, month, actualAmount(BigDecimal))
- Mappers (`@Mapper` + `extends BaseMapper<T>`, verbatim template):
  - `src/main/java/com/library/cost/mapper/DepartmentMapper.java`
  - `src/main/java/com/library/cost/mapper/BusinessLineMapper.java`
  - `src/main/java/com/library/cost/mapper/ProjectMapper.java`
  - `src/main/java/com/library/cost/mapper/EmployeeMapper.java`
  - `src/main/java/com/library/cost/mapper/LaborCostMapper.java`
  - `src/main/java/com/library/cost/mapper/ProjectCostMapper.java`
- Test (verbatim from brief):
  - `src/test/java/com/library/cost/SeedDataMapperTest.java` — `@SpringBootTest`, one test `seedDataLoaded` asserting: department count = 2; labor_cost count = 24 and SUM(amount) = 414000.00; project_cost count = 24 and SUM(actual_amount) = 1590000.00.

No other files were modified (README.md, pom.xml, application.yml, CostApplication, ApiResponse, GlobalExceptionHandler, CostApplicationTests untouched; .git untouched; no files added beyond the 15 above).

## Verification

Command (run twice, per constraints):
`cd /root/.agentix/agentic-dev/runs/DEV-f4ad1a6e-7360-11f1-8c66-df5563d236aa-06a25bdb-8cf6-43bb-b194-c140a741a358/worktree/library-backend-main && mvn -q test`

- **Run 1 (verbatim brief files): FAILED** — `Failed to load ApplicationContext` for both test classes.
  - Root cause (from surefire/log): `org.h2.jdbc.JdbcSQLSyntaxErrorException: Syntax error in SQL statement "CREATE TABLE labor_cost ( ... [*]month VARCHAR(7) NOT NULL ..."; expected "identifier" [42001-224]` — **`MONTH` is a reserved keyword in H2 2.x** (H2 2.2.224, managed by Spring Boot 3.2.5). The brief's verbatim schema column `month` cannot be created unquoted. The same keyword conflict would also break unquoted `SELECT ... month ...` generated by MyBatis-Plus, so quoting DDL/DML alone was insufficient.
- **Fix (smallest root cause, no contract change):** prepended 2 lines to `schema.sql`:
  ```
  -- H2 2.x reserves MONTH as a keyword; allow it as the column identifier (matches brief schema verbatim otherwise).
  SET NON_KEYWORDS MONTH;
  ```
  This is an H2 database setting that makes `MONTH` usable as a normal identifier, so the brief's DDL/DML/entities/mappers/test remain byte-for-byte otherwise. Validated standalone first against the exact H2 2.2.224 jar (`org.h2.tools.RunScript` with verbatim schema+data + MyBatis-Plus-style unquoted selects; all DDL/DML/selects and SUMs OK), then:
- **Run 2: BUILD SUCCESS** — `mvn -q test` exit code 0, ~14 s elapsed.
  - surefire per-test counts (from `target/surefire-reports/*.txt` and `.xml`):
    - `com.library.cost.CostApplicationTests` — **Tests run: 1, Failures: 0, Errors: 0, Skipped: 0** (contextLoads)
    - `com.library.cost.SeedDataMapperTest` — **Tests run: 1, Failures: 0, Errors: 0, Skipped: 0** (seedDataLoaded)
    - Total: **Tests run: 2, Failures: 0, Errors: 0, Skipped: 0**

## Deviations from brief

1. `schema.sql` has 2 additional header lines (`SET NON_KEYWORDS MONTH;` + explaining comment) — required because the brief's verbatim column name `month` is a reserved keyword in H2 2.x and the verbatim file cannot boot the app. This is the only content deviation; all tables/columns/indexes/DDL and all other 14 files match the brief verbatim.
2. Brief Step 7 (git commit) is **skipped** per the controlling session's hard constraint (GIT READ-ONLY). No git commands were run.
3. Note: the brief says "新增测试 2 项" — the deliverable is 2 test classes each with 1 @Test method (contextLoads + seedDataLoaded), which matches "Tests run: 2".

## Concerns / follow-up

- The `SET NON_KEYWORDS MONTH;` statement is H2-specific. If the app is ever deployed on MySQL/PostgreSQL, schema.sql needs a small porting change (e.g., backtick/quoted `month` or a renamed column with `@TableField`). Fine for the current H2-mem setup.
- The `month` column name collides with H2 keyword; any future SQL written by hand in native SQL (mappers with @Select) must keep quoting/keyword-awareness in mind.
- Baseline acceptance numbers asserted by the test (414000.00 labor, 1590000.00 actual, counts 24/24, dept count 2) all pass and are the intended Task 3/4 anchors.